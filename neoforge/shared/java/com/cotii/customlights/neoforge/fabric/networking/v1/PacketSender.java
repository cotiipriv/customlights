package com.cotii.customlights.neoforge.fabric.networking.v1;

/** Forge build: stands in for Fabric's packet sender in connection callbacks (the shared code never uses it). */
public interface PacketSender {
}
